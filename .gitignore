node_modules
# Keep environment variables out of version control
.env

/generated/prisma

# Uploaded files
uploads/*
!uploads/.gitkeep

# Local Whisper / Hugging Face hub cache (large binaries; never push to GitHub)
whisper-cache/
